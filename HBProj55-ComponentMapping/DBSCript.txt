CREATE TABLE "SYSTEM"."PERSONAL_INFO" 
   (	"PID" NUMBER(10,0) NOT NULL ENABLE, 
	"PNAME" VARCHAR2(20 BYTE), 
	"ADDRS" VARCHAR2(20 BYTE), 
	"COMAPANY" VARCHAR2(20 BYTE), 
	"DEPTNO" NUMBER(4,0), 
	"DESG" VARCHAR2(10 BYTE), 
	"SALARY" FLOAT(126), 
	 CONSTRAINT "PERSONAL_INFO_PK" PRIMARY KEY ("PID"));
	 